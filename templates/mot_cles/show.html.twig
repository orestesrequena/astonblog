{% extends 'base.html.twig' %}

{% block title %}MotCles{% endblock %}

{% block body %}
    <h1>MotCles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mot_cle.id }}</td>
            </tr>
            <tr>
                <th>Mot_cle</th>
                <td>{{ mot_cle.motCle }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ mot_cle.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mot_cles_index') }}">back to list</a>

    <a href="{{ path('mot_cles_edit', {'id': mot_cle.id}) }}">edit</a>

    {{ include('mot_cles/_delete_form.html.twig') }}
{% endblock %}
